fn main() {    
    let x: i32 = 2;  // 'i' means integer and '32' means 32 bit
                     // i32 is the default data types in implicit integers
    println!("{}", x);
    /* i8
     * i16
     * i32
     * i64
     * i128
     */
    let y: u32 = 4; // unsigned integers cant have negatives
                    
    println!("{}", y);
}
